# 更新紀錄

這裡記錄 VPS Sentinel 從一套輕量監控工具，慢慢長成完整 Home Assistant VPS 管理體驗的過程。

每個版本先說明它替使用者帶來了什麼，再保留必要的技術細節。除非特別標示，版本更新不會主動開啟遠端維護，也不會把 Home Assistant、MQTT 或其他服務公開到公網。

---

## 0.9.6 — 讓 iPhone 上最後一點突兀也消失

這個小版本專注在觸控後的視覺收尾，讓卡片被點開之後，不會留下像是鍵盤操作才會出現的焦點外框。

### 改善

- iPhone 以觸控開啟卡片詳細資訊後，會自動移除不必要的焦點狀態。
- 邊緣光保留原本的淡出動畫，不會突然消失。
- 鍵盤使用者仍能看到完整焦點提示，無障礙操作不受影響。

## 0.9.5 — 所有卡片，終於用同一種方式回應你

統一手機上的卡片互動，移除容易殘留的滑鼠懸停效果，讓每次按壓都有一致、柔和的回饋。

### 改善

- 資源卡與系統資訊卡改用同一套按壓光暈狀態。
- 移除手機觸控後可能殘留的 `hover` 視覺效果。
- 邊緣光在按下時淡入，詳細資訊開啟後平順淡出。

## 0.9.4 — 補齊系統資訊卡的互動一致性

修正四格系統資訊卡仍沿用舊觸控規則的問題。

### 修正

- 系統資訊卡現在與 CPU、記憶體及磁碟卡共用相同的按壓、過場與觸控邏輯。

## 0.9.3 — 更安靜、更自然的操作回饋

這個版本讓維護區與資訊卡的動畫不再搶戲，只在真正需要時提醒你。

### 改善

- 資源卡與系統資訊卡統一為深色縮放回饋，不再出現突兀的亮色反白。
- 維護提示改為柔和膠囊淡入淡出與低頻光澤動畫。
- 冷卻期間先顯示簡潔提示，最後 5 秒才進入數字倒數。
- 倒數只在必要時更新，不增加 Home Assistant 或 VPS 的日常輪詢負擔。

## 0.9.2 — 手機卡片開始真正「有回應」

補上資訊卡在手機上的按壓動畫與鍵盤操作支援，並修正前端快取造成的新舊版本混用。

### 改善

- 系統資訊卡加入明確但克制的觸控動畫。
- 資源卡與資訊卡採用一致的互動方式。
- 補上鍵盤操作與焦點提示。

### 修正

- 更新前端資源版本，降低 Home Assistant App 沿用舊快取的機率。

## 0.9.1 — 維護操作不再讓人猜發生了什麼

讓主機維護區更容易理解，也改善小螢幕上的按鈕與資訊排列。

### 改善

- Apple 風格面板加入輕量、平滑的卡片互動動畫。
- 維護操作統一在同一列顯示執行中、成功與失敗狀態。
- 調整手機版間距、字級與元件對齊。

### 修正

- 更新前端資源版本，避免 App 持續使用舊版介面。

## 0.9.0 — 里程碑：從「看見」走向「安全維護」

VPS Sentinel 不再只顯示主機狀態。這個版本首次讓使用者能從 Home Assistant 進行少量、明確且受限制的維護操作。

### 新功能

- Apple 面板新增主機維護區。
- 可檢查可用套件更新。
- 可安裝 Ubuntu 安全更新。
- 可排程重新啟動 VPS。

### 安全設計

- 遠端維護預設關閉，必須由維護中心明確啟用。
- 僅允許三種預先定義的操作，不接受任意 Shell 指令。
- 每次操作都需要再次確認。
- 指令包含白名單、時間戳、防重播、單一工作鎖與五分鐘冷卻保護。
- 維護工作透過獨立的暫時性 systemd 服務執行，不降低監控服務原有的唯讀防護。

### 改善與修正

- 操作期間會清楚顯示執行結果。
- 修正狀態卡同時出現兩個綠點的問題。
- 前端資源改用明確版本網址，避免 App 沿用測試版快取。

## 0.8.1 — 正式版不該被舊快取藏起來

### 修正

- 修正 Home Assistant App 可能沿用 RC6 前端資源，導致正式版版面沒有更新。
- 0.8 系列統一使用固定資源網址 `?v=0.8`，減少修補版頻繁更換資源網址的負擔。

## 0.8.0 — 里程碑：Apple 風格儀表板登場

這是 VPS Sentinel 視覺體驗的一次大幅重製。它不只是把數字排得更漂亮，而是嘗試讓一台遠方的 VPS，在手機裡看起來像一個真正被照顧的裝置。

### 新功能

- 新增可選的 Apple 風格自適應資源卡。
- 保留 0.7 原生儀表板作為穩定備援，不強迫使用自訂卡片。
- 顯示節點國旗、VPS 供應商與本機作業系統。

### 視覺與體驗

- 使用 iOS 系統字體、毛玻璃材質、連續圓角與分層文字。
- CPU、記憶體與磁碟採用 CSS 自適應網格與平滑數值動畫。
- 自動配合深色、淺色與「減少動態效果」偏好。

### 輕量與隱私

- 前端元件約 10 KB，零外部依賴。
- 不增加 VPS 常駐程序。
- 外部 IP 資訊只在服務啟動時查詢一次，且不會發布公網 IP。

### 安全安裝

- 使用 Home Assistant 官方資源註冊流程，不直接修改 `.storage`。
- 套用前先備份並驗證；若失敗，會自動回復原生儀表板。

## 0.7.0 — 里程碑：真正為手機而生的原生儀表板

重新設計 Home Assistant 原生面板，讓同一份儀表板能自然適應手機、平板與桌面。

### 新功能與改進

- 採用 Home Assistant 原生 Sections 自適應版面。
- 手機自動切換為單欄，平板與電腦最多展開三欄。
- CPU、記憶體與磁碟改為同層級的直式 Tile 與動態長條。
- 手機版使用三張緊湊橫向資源卡，避免單一卡片占滿畫面。
- 點選資源卡可直接開啟 Home Assistant 原生詳細資料。

### 資訊層級

- 資源、健康與系統資訊使用不同低透明度色彩分區。
- 正常時只保留必要狀態，避免滿畫面都是「正常」。
- 服務異常、資料停止與重新啟動提醒只在需要時出現。
- 狀態依嚴重程度使用綠、藍綠、橘與紅色。

### 相容性

- 不依賴 HACS、第三方卡片或額外前端常駐程式。

## 0.6.2 — 手機介面再瘦身

### 改善

- 移除手機終端容易錯位的字元框線，改用簡潔穩定的標題。
- CPU、記憶體與磁碟改為同層級、同尺寸的主要儀表。
- 移除重複的容量說明，讓手機面板更乾淨。

## 0.6.1 — 修好升級路上的一道門

### 修正

- 修正升級工具比較 Release 版本時的 Bash 換行語法錯誤。
- 避免舊版使用者完成下載後，仍無法進入安裝流程。

## 0.6.0 — 里程碑：更即時，卻沒有更吵

重新設計監控資料的更新節奏，讓 CPU 與記憶體更即時，同時避免完整健康檢查頻繁消耗資源。

### 監控架構

- MQTT 回報拆分為快速資源與低頻狀態兩條資料流。
- 平衡模式每 15 秒更新 CPU 與記憶體。
- 完整健康檢查維持每 5 分鐘執行。
- 保留既有實體 ID 與舊版 MQTT 主題，降低升級中斷風險。

### 新增資訊

- 顯示記憶體與磁碟實際容量。
- 新增整體健康狀態。
- 以「資料持續更新」取代不直覺的最近回報時間。

### 可靠性與體驗

- 資料停止更新時，即時實體會自動轉為不可用。
- 重製手機優先儀表板。
- 異常提醒只在需要處理時置頂，正常時維持安靜簡潔。

## 0.5.0 — 里程碑：維護中心成為真正的日常入口

將原本分散的管理工具整理成一致、清楚且更不容易誤操作的中文維護體驗。

### 維護中心

- 整理為系統總覽、監控設定、Home Assistant 與系統維護四個工作區。
- 主畫面直接顯示版本、監控服務與 MQTT 即時狀態。
- 統一選單編號、返回行為、狀態圖示與完成提示。
- 健康檢查、備份、自動化與移除工具統一使用繁體中文介面。

### 新增指令

- `status`
- `settings`
- `dashboard`
- `doctor`
- `backup`
- `upgrade`
- `ha-update`

### 安全與可靠性

- 完整移除的預設選項改為取消，避免誤按 Enter。
- 危險操作移至系統維護區，執行前提供用途說明。
- 更新前清楚說明備份、驗證與失敗回復流程。
- 更新成功後只保留最近一份專案回復備份。
- 清理範圍只限 VPS Sentinel 建立的內容，不執行全域 Docker 清理。
- 事後清理失敗只顯示提醒，不會把成功更新誤判為失敗。

### 自動化支援

- 支援 `NO_COLOR`。
- 支援非互動式狀態查詢，方便 SSH 紀錄與自動化使用。

## 0.4.1 — 舊資料也能溫柔地被接住

### 修正

- MQTT 舊保留資料缺少 `last_report` 時，不再產生模板警告。
- Discovery 模板在欄位暫時缺少時會安全顯示為未知。
- 避免 Home Assistant 重新啟動期間的舊狀態，讓新感測器被錯誤標記為不可用。

## 0.4.0 — 里程碑：診斷、備份與通知正式成形

讓使用者在問題出現時，不必先成為 Linux 專家，才能知道哪裡出了錯。

### 新功能

- 新增按需執行的一鍵健康檢查，不增加常駐監控負擔。
- 新增經使用者確認的安全修復流程。
- 可產生匿名診斷報告。
- 更新前檢查磁碟空間與安裝完整性。
- 新增輕量設定備份、備份清單、保留策略與還原驗證。

### Home Assistant 自動化

- 新增系統異常 Blueprint。
- 新增主機離線 Blueprint。
- 新增每日摘要 Blueprint。
- Blueprint 不修改 `.storage`，通知動作仍由使用者自行選擇。

### 更新與移除

- 升級與失敗回復涵蓋新增管理工具及模板。
- 完整移除會清理本專案建立的診斷報告與手動備份。

## 0.3.0 — 里程碑：從腳本集合變成一個產品

這個版本加入統一入口、升級、儀表板與設定工具，VPS Sentinel 從安裝後就不再需要被遺忘。

### 新功能

- 新增 `vps-sentinel` 中文維護中心。
- 新增安全升級工具，包含下載驗證、備份與啟動失敗回復。
- 可安全切換資源模式與調整告警門檻。
- 設定失敗時會自動回復原設定。
- 可產生只使用 Home Assistant 內建卡片的 VPS Sentinel 儀表板。
- 儀表板安裝前會備份並驗證 Home Assistant 設定，失敗時自動回復。
- 維護中心整合 Home Assistant 更新與完整移除工具。

## 0.2.1 — 移除，也應該是一件讓人安心的事

### 新功能

- 新增中文互動式移除工具。
- 可選擇只移除監控器，或完整清除專案環境。

### 安全設計

- 完整移除前預設建立最終備份。
- 必須輸入指定確認文字，避免誤刪。
- 只移除專案專用的 MQTT 帳號、設定、Tailscale Serve 與固定路徑。
- 共用套件採保守偵測與二次確認，保護其他 MQTT、Docker 與 SSH 用途。

## 0.2.0 — 私有連線與可靠更新的基礎

### 網路與安全

- Home Assistant 改用 Tailscale Serve 提供 tailnet 私有 HTTPS。

### 更新可靠性

- 新增設定驗證、備份、啟動檢查與映像回退。
- 監控程式更新時略過未變更的 Python 依賴，並確保服務重新啟動。

### 狀態呈現

- 無法讀取安全更新或 Docker 狀態時，改為顯示未知而非錯誤資訊。
- Debian 裝置顯示實際作業系統名稱。

### 開發品質

- CI 新增完整 ShellCheck。
- 新增 Python 單元測試。
- 啟用 Dependabot。

## 0.1.0 — 一切開始的地方

VPS Sentinel 首次公開。目標很簡單：讓使用者不用長時間盯著 SSH，也能在 Home Assistant 裡知道自己的 VPS 是否安好。

### 首次提供

- 中文一條龍安裝流程。
- MQTT Discovery 自動建立實體。
- Home Assistant 整合。
- 選配 HomeKit 支援。
- 低資源監控模式。
- MQTT 自動重連。
