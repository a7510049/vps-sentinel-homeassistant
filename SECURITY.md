# 安全政策

## 支援範圍

安全修正以 `main` 分支的最新版本為準。舊版、分支版本及自行修改的
部署不保證提供安全更新。

## 回報安全問題

請勿使用公開 Issue 回報尚未修正的弱點、憑證或可被利用的設定。
請透過本 repository 的 **Security → Report a vulnerability** 私下提交
安全通報，並附上：

- 受影響的版本或 commit
- 問題的重現條件與可能影響
- 已知的緩解方式（若有）

維護者會先確認收到通報，再評估修正與揭露時程。請在修正發布前避免
公開技術細節。

## 部署責任

本專案預設將 Mosquitto 限制在 `127.0.0.1`。使用者若變更監聽範圍，
應自行配置 TLS、強密碼、網路存取控制及定期更新。請勿將未加密的
MQTT `1883` 連接埠直接公開至網際網路。
